# 🔐 Exemplo de Variáveis de Ambiente
# 
# Este arquivo serve como template para as variáveis de ambiente necessárias.
# NÃO COMMITE ESTE ARQUIVO COM VALORES REAIS!
# 
# Use este arquivo como referência para configurar os Secrets no GitHub.

# ================================
# 🖥️ AZURE VM
# ================================

# IP público ou hostname da VM Azure
AZURE_VM_HOST=20.123.45.67

# Usuário SSH para conexão
AZURE_VM_USERNAME=azureuser

# Porta SSH (padrão: 22)
AZURE_VM_SSH_PORT=22

# Chave privada SSH (conteúdo completo do arquivo)
# Obtenha com: cat ~/.ssh/azure_vm_deploy
AZURE_VM_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAACmFl...
(cole aqui o conteúdo completo da chave privada)
...
-----END OPENSSH PRIVATE KEY-----

# ================================
# 🗄️ MYSQL DATABASE
# ================================

# Senha do root do MySQL (use uma senha forte!)
# Gere com: openssl rand -base64 20
MYSQL_ROOT_PASSWORD=Pr1m0rd14l@Duck!2025Strong

# Nome do banco de dados
MYSQL_DATABASE=primordial_duck_operation

# ================================
# 🔐 JWT AUTHENTICATION
# ================================

# Chave secreta para assinatura JWT (mínimo 32 caracteres)
# Gere com: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long-random-string

# Emissor dos tokens JWT
JWT_ISSUER=PrimordialDuckOperation

# Audiência válida para os tokens
JWT_AUDIENCE=PrimordialDuckOperationUsers

# Tempo de expiração em minutos
JWT_EXPIRATION_MINUTES=60

# ================================
# 🎨 FRONTEND - VITE
# ================================

# URL base da API (substitua pelo IP ou domínio da sua VM)
# IMPORTANTE: Incluir /api no final!
VITE_API_URL=http://20.123.45.67:7000/api

# ================================
# 📧 EMAILJS (Opcional)
# ================================
# Obtenha em: https://dashboard.emailjs.com/

# Service ID do EmailJS
VITE_EMAILJS_SERVICE_ID=service_xxxxxxx

# Template ID do EmailJS
VITE_EMAILJS_TEMPLATE_ID=template_xxxxxxx

# Public Key do EmailJS
VITE_EMAILJS_PUBLIC_KEY=xxxxxxxxxxxxxxx

# ================================
# 📝 NOTAS IMPORTANTES
# ================================

# 1. NUNCA commite este arquivo com valores reais!
# 2. Use senhas fortes e únicas
# 3. Rotacione secrets regularmente
# 4. Guarde backups seguros das chaves
# 5. Use GitHub Secrets para armazenar valores sensíveis

# ================================
# 🛠️ FERRAMENTAS PARA GERAR VALORES
# ================================

# Gerar JWT Secret (Linux/Mac/Git Bash):
# openssl rand -base64 32

# Gerar JWT Secret (PowerShell):
# [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes([System.Guid]::NewGuid().ToString() + [System.Guid]::NewGuid().ToString()))

# Gerar Senha Forte (Linux/Mac):
# openssl rand -base64 20

# Gerar Senha Forte (PowerShell):
# -join ((33..126) | Get-Random -Count 20 | ForEach-Object {[char]$_})

# Gerar Chave SSH (Linux/Mac/Windows):
# ssh-keygen -t rsa -b 4096 -C "github-deploy" -f ~/.ssh/azure_vm_deploy

# ================================
# ✅ CHECKLIST
# ================================

# [ ] Todos os valores foram substituídos
# [ ] Senhas são fortes e únicas
# [ ] Chaves SSH foram geradas
# [ ] IP da VM está correto
# [ ] URL da API inclui /api
# [ ] EmailJS configurado (se usar)
# [ ] Secrets adicionados no GitHub
# [ ] Este arquivo NÃO foi commitado com valores reais

# ================================
# 🔗 LINKS ÚTEIS
# ================================

# Portal Azure: https://portal.azure.com
# GitHub Secrets: https://github.com/SEU_USUARIO/SEU_REPO/settings/secrets/actions
# EmailJS Dashboard: https://dashboard.emailjs.com/
# Docker Hub: https://hub.docker.com/
# GitHub Container Registry: https://github.com/features/packages

# ================================
# 📞 SUPORTE
# ================================

# Documentação: .github/workflows/README.md
# Quick Start: .github/workflows/QUICKSTART.md
# Guia de Secrets: .github/workflows/SECRETS.md
# Checklist: .github/workflows/CHECKLIST.md
